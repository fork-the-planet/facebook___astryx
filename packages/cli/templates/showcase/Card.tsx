'use client';

import {XDSCard} from '@xds/core/Card';

export default function CardShowcase() {
  return (
    <XDSCard width={300}>
      <p style={{margin: 0}}>Simple content inside a card.</p>
    </XDSCard>
  );
}
