---
'@xds/core': patch
---

`XDSMarkdown` now supports `display="inline"` for rendering inline markdown spans that inherit surrounding typography. Use it for doc text and table cells that need markdown formatting without block-level wrappers.
